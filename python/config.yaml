default:
  mode: offline
  interactive: False
  sandbox: '0.0'
  compatibility: forward

offline:
  schema_registry: localhost
  broker: localhost:9092
  registry_url: http://localhost:8081
  db: "" # localhost:11001


event:
  topics:
    - first: xavier
      last: mayeur
    - first: sukumar
      last: daniel


stream:
  topics: ('')

consumer:
  topics:
    - 'itpcEvent'

